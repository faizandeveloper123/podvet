# Server / database
WEB_PORT=8080
# WEB_USER_DATA_DIR=/root/.podvet

# MySQL – PodVet uses its OWN isolated database, never the shared podvet one.
DB_HOST=localhost
DB_PORT=3306
DB_USER=podvet
DB_PASSWORD=CHANGE_ME
DB_NAME=podvet
DB_PREFIX=podvet_clinic_
CLINIC_PREFIX=podvet_clinic_
DB_SSL=false